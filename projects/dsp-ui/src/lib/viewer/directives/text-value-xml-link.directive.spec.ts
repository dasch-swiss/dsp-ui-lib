import { TextValueXmlLinkDirective } from './text-value-xml-link.directive';

describe('TextValueXmlLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TextValueXmlLinkDirective();
    expect(directive).toBeTruthy();
  });
});
