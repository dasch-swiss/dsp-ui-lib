import { TextValueLinkDirective } from './text-value-link.directive';

describe('TextValueLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TextValueLinkDirective();
    expect(directive).toBeTruthy();
  });
});
