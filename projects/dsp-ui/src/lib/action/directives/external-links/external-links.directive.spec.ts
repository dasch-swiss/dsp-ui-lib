import { ExternalLinksDirective } from './external-links.directive';

describe('ExternalLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalLinksDirective();
    expect(directive).toBeTruthy();
  });
});
